{
  "name": "tradecompound",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first Indian options trading journal for NSE and BSE traders.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/app.js && node --check sw.js"
  },
  "engines": {
    "node": ">=20"
  }
}
